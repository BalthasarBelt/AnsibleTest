---
- name: Configure nginx web server
  hosts: ansibletest
  tasks:
  - name: install nginx server
    yum:
        update_cache: yes
        upgrade: yes
    yum:
      name: nginx
      state: latest
  - service:
      name: nginx
      state: started